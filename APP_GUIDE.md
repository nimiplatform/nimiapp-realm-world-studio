# Realm World Studio

与 AI 一起构建和维护世界，让角色拥有清晰的身份、目标、声音与边界。

## 使用方式

1. 在「我的世界」写下一段灵感，点击「展开这个想法」，或从空白开始。
2. 第一次使用 AI 时，进入「AI 设置」选择 Nimi 本机模型或已配置的云端模型。
3. 在世界手册中填写概念、基础设定、规则、地点与角色构想。右侧的 AI 可以完善世界、构想角色或检查一致性。
4. AI 建议不会直接改动你的作品。逐项点击「加入草稿」或替换对应内容，随后继续编辑；采纳后、下一次编辑前可以撤销。
5. 在「审阅与保存」检查完整内容，选择可见范围并确认审阅，再创建到 Realm。默认仅自己可见。
6. 打开已有 Realm 世界，点击「起草改进方案」。修改后再次审阅，点击「保存修改到 Realm」。如果世界已被更新，先比较最新版本，再决定是否基于它重新审阅自己的草稿。
7. 世界保存后，可将手册中的角色构想继续创建为世界角色，也可在世界页点击「添加世界角色」。填写人物设定及明确的行为、表达和边界；可以请 AI 起草，确认采纳后仍需审阅。
8. 角色保存后可打开详情、编辑并保存新版本。尚未完成的角色草稿会列在所属世界中，可继续完善。

世界列表最多显示最近 50 个可访问世界，支持搜索与可见范围筛选。世界资料以 Realm 返回的正式记录为准。

世界创建由管理员向指定账号开放；Halliday 是初始授权的世界创作者。未获得创建权限时仍可写草稿，维护已有世界则继续按所有权判断。创建世界只保存该世界的设定、规则和地点，角色需要单独审阅并创建。

世界角色是属于这个世界的角色设定。保存后，可以查看已保存的版本，继续修改人物资料、表达方式和行为边界；重新打开时，以 Realm 保存的内容为准。

AI 的创意质量取决于所选模型。建议指出具体修改目标，例如「这条规则有什么代价」或「给两位角色相互冲突的目标」，再审阅结果。

如果生成失败，原文和之前的建议会保留。候选格式不完整时，可以选择「请 AI 整理这份候选」，或重新生成；整理结果同样需要你审阅。如果显示「草稿未保存」，请保持窗口打开并重试；也可以复制世界手册。只有收到 Realm 返回的正式记录，界面才会提示世界创建成功。

## 运行条件

在 Nimi 的应用列表中安装或导入此应用，再点击启动。应用由 Nimi 启动和管理；运行状态、App Access 和所需能力以 Nimi 的实际状态为准。

访问 Realm 数据需要 Nimi 中有效的账号与相应访问条件。

## 数据与支持

创作草稿、角色构想和 AI 候选通过 Nimi 的 protected storage 保存，始终不等于 Realm 正式资料。应用另在本机保存语言偏好，以及 Electron/Chromium 运行所需的设置与缓存。缓存占用会随使用情况变化。从 Nimi 卸载应用后，OS 本地文件仍会保留。

[使用与开发说明](https://github.com/nimiplatform/nimiapp-realm-world-studio) · [反馈问题](https://github.com/nimiplatform/nimiapp-realm-world-studio/issues)
